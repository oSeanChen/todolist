<!DOCTYPE html>
<html>
  <head>
    <title>Todolist</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <%= render 'layouts/header' %>
    <% if notice  %>
      <p class="notice bg-blue-100 rounded-lg py-5 px-6 mb-4 text-base text-blue-700 mb-3"><%= notice  %></p>
    <% end %>
    <% if alert %>
      <p class="alert bg-red-100 rounded-lg py-5 px-6 mb-4 text-base text-red-700 mb-3"><%= alert if alert %></p>
    <% end %>
    <%= yield %>
  </body>
</html>
